export enum DepthLevels {
  BOTTOM = 0,
  CHARACTERS_LEVEL = 1,
  MEDIUM = 2,
  TOP = 3,
  OMNISCENT = 4,
}

export enum IconSelectable {
  POKEMON,
  POKEDEX,
  BAG,
  CARD,
  OPTIONS,
  SAVE,
}

export enum BagState {
  DISPLAY,
  COMMAND,
}

export enum PartyMenuState {
  DISPLAY,
  COMMAND,
  SWITCH,
  ASSIGN_ITEM,
  DISPLAY_TEXT,
  WAIT_SECOND_A,
  END,
}
